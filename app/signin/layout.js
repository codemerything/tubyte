import React from "react";
import { Poppins } from "next/font/google";
import Proxima from "next/font/local";

export default function layout({ children }) {
  return <section>{children}</section>;
}
